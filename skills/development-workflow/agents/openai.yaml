interface:
  display_name: "开发工作流"
  short_description: "对齐需求、实现验证、质量验收并按需沉淀开发知识"
  default_prompt: "Use $development-workflow to classify this development task, implement it with proportionate verification, run the artifact quality gate, and ask before creating a note."

policy:
  allow_implicit_invocation: true
